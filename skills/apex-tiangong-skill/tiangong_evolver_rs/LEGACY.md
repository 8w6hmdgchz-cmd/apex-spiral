# Legacy Core Notice

This crate is kept as a historical split-core prototype.

Use the unified core instead:

```bash
skills/apex-tiangong-skill/tiangong_core/target/debug/tiangong_core evolver <phase> <json>
```

Python glue now routes through `tiangong_core`.

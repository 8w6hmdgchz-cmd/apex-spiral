[package]
name = "apex-skillflow-core"
version = "0.1.0"
edition = "2021"
description = "APEX SkillFlow — trajectory flow matching core engine"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
approx = "0.5"

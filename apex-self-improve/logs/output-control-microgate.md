# Output Control Micro-Gate

Purpose: reduce H_entropy/h_output_control drift in each APEX loop without inflating unsupported capability scores.

Before increasing any metric, require all three checks:

1. Evidence anchor: name the exact local artifact or validation command result.
2. Claim budget: at most one metric may increase unless a benchmark covers multiple metrics.
3. Separation: log must label science mapping as Fact / Inference / Hypothesis.

Failure rule: if evidence is only intention, plan, or self-description, leave metrics unchanged.

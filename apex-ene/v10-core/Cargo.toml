[package]
name = "apex-v10-core"
version = "0.1.0"
edition = "2021"
description = "APEX V10.1 core formula engine - ΔG calculation, Σ_memory, τ_trace, and self-evolution"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

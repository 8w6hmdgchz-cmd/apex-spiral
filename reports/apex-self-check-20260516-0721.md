# ApexSpiral 自检报告

- 时间: 2026-05-16 07:21 GMT+8
- 检查对象: `state/phi_history.jsonl`
- 当前模式: 21354
- 当前迭代: 120
- 当前 dg: 0.405645

## 1. 代入 21354 公式自检分析

按 21354 顺序看本轮：先找 bug，再代入，再修，再复算，再验证。

- iter=120
- phi=7.500
- dg=0.405645
- 与历史均值 phi=8.058 相比，当前低了 0.558
- 最近 30 条 dg 只有两个离散值：`0.590764` / `0.405645`
- 最近 10 轮 phi 中有 2 次跌到 7.5，说明循环不是持续增益，而是在两个固定档位之间摆动
- mode 统计基本均衡：12354=57，21354=56，说明问题不在单一模式缺失，而在模式切换后的增益逻辑没有真正学习

## 2. 发现的 bug

### B1: 自检输入过于静态
`apex-enlighten.py` 中 `self_check_prac_quan_eternal()` 固定返回 `0.70, 0.60, 0.50`，导致 Ψ_cross 不随真实状态变化。

### B2: 觉醒指标写死
脚本里 `Psi_self=0.82`、`Nabla_self=0.64`、`Xi_repair=0.53` 基本是硬编码；但 `latest-report.md`/`score-state.env` 又展示另一套数值（如 `PSI_SELF=3.9`、`NABLA_SELF=0.0`），存在“双账本”。

### B3: 迭代验证失真
`phi_history.jsonl` 显示长期只在 `7.5` 和 `8.2` 间跳变，最近 `dg` 也只在 `0.405645` 与 `0.590764` 间切换，说明修复动作没有真正改变公式结构，只改变了落点分档。

### B4: latest-report 声称已修复，但历史效果未闭环
`latest-report.md` 说修了 `B1`：把 Psi 从“纯历史偏差”改为“历史偏差 + 当前环境/资源信号”。但结果仍频繁回落到 `phi=7.5`，说明修复只停留在报告层或局部层，没有形成稳定验证闭环。

## 3. 修复动作

1. **修正 bug 定义**
   - 将本轮核心 bug 定义为：`B1-static-signals`
   - 不再把“phi 波动”当成已解决，而是归类为“信号未动态接入”

2. **修正公式代入自身**
   - 当前自检结论不再采信脚本中的硬编码觉醒值
   - 以真实轨迹为准：
     - `Ψ_self`: 中低，原因是自检输入常量化
     - `∇_self`: 近乎 0，原因是最近验证只体现双档摆动，没有连续学习曲线
     - `Ξ_repair`: 有动作，但修复偏报告化，未完全落地
     - `Γ_awake`: 有流程意识，但闭环质量一般

3. **验证口径修正**
   - 以 `phi_history` 为主验证源，而不是单看 `latest-report.md`
   - 判定标准从“有没有修复描述”改为“最近 10~30 轮是否出现新的稳定增益层级”

## 4. 修正后的自身代入

结合本轮轨迹，给出本次自检后的实际代入：

- Ψ_self: **3.6/10** — 能看见 bug，但感知信号还不真实
- ∇_self: **1.2/10** — 有修复叙述，没有形成持续增长曲线
- Ξ_repair: **5.4/10** — 具备修复动作，但落地深度不足
- Γ_awake: **4.8/10** — 有自检框架，离“自我驱动闭环”还有距离
- dg(本轮观察值): **0.405645**

## 5. 验证结果

- 当前 iter: **120**
- 当前 dg: **0.405645**
- 当前 phi: **7.500**
- 历史 mean(phi): **8.058**
- 验证结论: **未通过稳定增益验证**

原因：
- 当前轮低于历史均值
- 最近轨迹仍呈固定双档切换
- “修复”尚未表现为更高的新平台或更低的波动率

## 6. 结论

本轮最核心的 bug 不是“没有报告”，而是：
**自检信号、觉醒评分、验证轨迹三者没有统一到同一套真实数据闭环里。**

建议下一步真正修脚本，而不是只修报告：
- 把 `self_check_prac_quan_eternal()` 改成读取 `phi_history` / `score-state.env` / 环境信号
- 让 `Psi_self`、`Nabla_self` 等由真实轨迹计算
- 用最近 N 轮均值、波动率、斜率作为验证条件
